#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../npm-license/bin/npm-license" "$@"
  ret=$?
else 
  node  "$basedir/../npm-license/bin/npm-license" "$@"
  ret=$?
fi
exit $ret
