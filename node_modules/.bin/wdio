#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webdriverio/bin/wdio" "$@"
  ret=$?
else 
  node  "$basedir/../webdriverio/bin/wdio" "$@"
  ret=$?
fi
exit $ret
